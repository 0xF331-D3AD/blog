name: Build and Deployment
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
          find ~/ -type f -iname "package.json" 2>/dev/null
      - run: echo "🍏 This job's status is ${{ job.status }}."
