name: Build and Deployment
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
          ls -la
      - run: echo "🍏 This job's status is ${{ job.status }}."
